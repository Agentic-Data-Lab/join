# 只读使用规范

加入 Agentic Data Lab 组织后，你拿到的是**只读**权限。为了让这件事长期成立，请遵守以下几条。

## 允许

- 阅读组织内你被授权访问的仓库（目前是 `server_info` 服务器文档仓库）；
- **让自己的 agent / 脚本读取这些 Markdown 文档**，用来帮你写代码、连服务器、排查环境问题；
- 在自己机器上保存副本、做检索索引（仅限个人使用）。

## 不允许

- 把文档内容**公开发布**（博客、公众号、公开仓库、论坛、群文件等）；
- 把文档里的**账号、口令、IP、跳板机信息**转发给组外的人；
- 用组织成员身份去修改、删除仓库内容，或给自己/他人提权；
- 用自动化手段高频抓取（正常读取请走 git clone 或 GitHub API，不要写爬虫刷页面）。

## 说明

- 文档面向实验室内部，里面包含内网地址、共享盘口令等不宜外传的信息；
- 你可以在组织内看到其他成员的姓名与账号，这属于内部信息，同样不对外；
- 发现文档有错、有更新需求：告诉管理员，或直接在组织里发起讨论，不要自行改。

## 联系

有问题（权限、密码、文档错误）找管理员：俞鉴珂。
