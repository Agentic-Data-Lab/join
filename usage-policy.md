# 只读使用规范

加入 Agentic Data Lab 组织后，你拿到的是**只读**权限。为了这件事能长期成立，请遵守以下几条。

## 适用范围

**组织内所有私有（private）仓库**——不只服务器文档，还包括以后陆续加入的其他仓库。
其中任何内容都属于实验室内部资料，默认不外传。公开（public）仓库不受此限制。

## 允许

- 阅读组织内你被授权访问的仓库；
- **让自己的 agent / 脚本读取这些仓库的内容**（例如用你的 GitHub 凭据 `git clone`），用来帮你写代码、连服务器、排查环境问题；
- 在自己机器上保存副本、做检索索引（仅限个人使用）。

## 不允许

- 把组织内私有仓库的内容**公开发布**（博客、公众号、公开仓库、论坛、群文件、组外群等）；
- 把其中的**账号、口令、IP、跳板机信息**转发给组外的人；
- 用组织成员身份去修改、删除仓库内容，或给自己/他人提权；
- 用自动化手段高频抓取（正常读取请走 `git clone` 或 GitHub API，不要写爬虫刷页面）。

## 其他实验室资源

服务器之外的组内资源（各类在线服务、面板、工具入口）统一从导航面板进入：
**<https://panel.jianke-yu.online/>**。同样是内部资源，不对外传播。

## 说明

- 你能在组织内看到其他成员的姓名与账号，这属于内部信息，同样不对外；
- 发现文档有错、或有更新需求：告诉管理员，或在组织内发起讨论，不要自行改。

## 联系

有问题（权限、口令、文档错误）找管理员：俞鉴珂。
